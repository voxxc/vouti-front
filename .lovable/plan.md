Remover o botão e toda a lógica relacionada a "Re-buscar andamentos (Judit)" do componente `ProcessoOABDetalhes.tsx`.

### Alterações
1. Remover estado `rebuscandoAndamentos` (useState).
2. Remover função `handleRebuscarAndamentosJudit`.
3. Remover bloco JSX do botão "Re-buscar andamentos (Judit)" da seção de resumo.
4. Verificar se o ícone `RefreshCw` ainda é necessário nas importações.
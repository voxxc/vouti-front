Inserir simulação no caso 0005137-68.2026.8.16.0021 (tenant Demorais) via migração SQL: 6 andamentos em `processos_oab_andamentos` + 6 anexos em `processos_oab_anexos` reaproveitando os storage_paths reais dos 6 arquivos do request 0e27516b (4 PDFs + 2 HTMLs já no bucket processo-documentos). Vincular via `step_id` compartilhado entre andamento e anexo. Marcar `dedup_hash = 'SIM-...'` e `dados_completos.simulacao = true` para remoção trivial depois. 2 dos 6 com `tipo_movimentacao='Intimação'` para popular também a aba Intimações. Zero alteração de código — o subdrawer MovimentacaoDetalhe e useProcessoAnexos já leem essas tabelas.